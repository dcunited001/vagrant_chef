source :rubygems

# this project doesn't really require a bundle
gem 'vagrant'

# but this gem sounds so nice i couldn't resist
gem 'vagrant-hostmaster'

# until i get dnsmasq up & running
#   on my network of course
